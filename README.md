This project was created by Andreas Bismo for frontend task



Go to `client` directory and run:

### `npm install`
### `npm start`



Go to `api` directory and run:

### `npm install`
### `npm start`
